# HuggingFace model repos to pre-fetch into the submission container.
# One repo per line. The platform downloads these before the container starts.
#
# Keep in sync with the encoder used in train_modal.py, model.py, and
# labeling.py. Current pipeline uses all-mpnet-base-v2 (768-d sentence
# embeddings).
sentence-transformers/all-mpnet-base-v2
